import React from "react";

const SectionContainer = ({ children }) => {
  return (
    <div className="max-w-screen-2xl mx-auto md:px-[80px] ">{children}</div>
  );
};

export default SectionContainer;
